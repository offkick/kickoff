spring:
  datasource:
#    url: jdbc:h2:mem:~/kickOffApplication
    url: jdbc:mysql://localhost:3306/kick_off?serverTimezone=UTC&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
#  profiles:
#    active: domain